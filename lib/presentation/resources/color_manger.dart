import 'package:flutter/material.dart';

class ColorManger {
  //using in appbar and safe area and background
  static Color gradationLightBlue = const Color.fromRGBO(218, 240, 253, 1);

  static Color backGround = const Color.fromRGBO(232, 246, 255, 1);

//using in bottomSheet
  static Color lightBlue =
      const Color.fromRGBO(27, 152, 224, 0.8); //using in button
  static Color darkBlue = const Color(
      0xFF037AC2); // using in stack switch between login and sign up ,and about button

  static Color white = Colors.white; //using in button
  static Color lightWhite = Colors.white70; //using in button nav bar
  static Color black = Colors.black; //using in text
  static Color blackText =
      const Color(0xff4A4A4A); //using in text profile screen

  static Color blue =
      const Color(0xFF037AC2); //using in text button and profile button
  static Color darkGrey =
      const Color.fromRGBO(156, 156, 156, 1); //using in dot in on boarding
  static Color lightDarkBlue =
      const Color(0xFFB8E5FF); //using in profile container
  static Color lightBlackColor = const Color(0xff4A4A4A); //using text profile
  static Color lightWhiteColor =
      const Color(0xffF2F2F2); //using in card profile
  static Color lightBlueDegree =
      const Color(0xff81D1FF); // using in container profile
  static Color? lightYellow = Colors.yellow[800]; //using in icon profile

  static Color error = Colors.red; //using in error text form field
  static Color transparent =
      Colors.transparent; //using in container stack login and sign up

  static Color grey = Colors.grey;
  static Color? lightGrey = Colors.grey[600];
  static Color? lightGrey2 = Colors.grey[300];
  static Color lightBlack = Colors.black54;
  static Color lightBlue1 = const Color(0xFF99DAFF); //using in splash screen
  static Color darkBlue1 =
      const Color(0xFF1d2733); //using in back ground dark screen
  static Color blue1 = const Color(0xFF63a2d8); //using in back button
}
